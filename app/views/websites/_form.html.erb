<%= form_for(@website) do |f| %>
  <% if @website.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@website.errors.count, "error") %> prohibited this website from being saved:</h2>

      <ul>
      <% @website.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :before_image %><br>
    <%= f.file_field :before_image %>
    <%= f.hidden_field(:before_image_cache)%>
  </div>
  <div class="field">
    <%= f.label :after_image %><br>
    <%= f.file_field :after_image %>
    <%= f.hidden_field(:after_image_cache)%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  <%= hidden_field_tag :direct, params[:direct] %>
<% end %>
